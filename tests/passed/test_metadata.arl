archetype test_metadata

entry exec () {
  effect {
    const k = "";
    const v = 0x00;
    metadata.update(k, some(v));
  }
}

// entry addMetadata (ik : string, iv : bytes) {
//   called by admin
//   effect {
//     /* put(metadata, ik, iv) */

//     // metadata.put(ik, iv)
//     metadata.update(ik, some(iv));
//   }
// }

// entry removeMetadata(ik : string) {
//   called by admin
//   effect {
//     metadata := remove(metadata, ik)
//   }
// }

// entry setValue (v : nat) {
//   called by admin
//   effect {
//     value := v
//   }
// }

// getter getValue() : nat {
//   return value
// }
