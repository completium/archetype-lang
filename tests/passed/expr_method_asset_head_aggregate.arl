archetype expr_method_asset_head_aggregate

asset o_asset identified by oid {
  oid : int;
}

asset my_asset identified by id {
  id     : string;
  col    : aggregate<o_asset>;
} initialized by {
  {"id0"; []};
  {"id1"; []};
  {"id2"; []}
}

variable res : bool = true

entry exec () {
  var l = my_asset["id0"].col.head(2);
}
