archetype lang_methods_builtin_collection

action exec () {
  effect {
    var l : string collection = ["1"; "2"; "3"];

    l.add("4");
    l.remove("1");
    l.clear();
    l.reverse();

    var res0 : bool               = l.contains("2");
    var res1 : int                = l.count();
    var res2 : string collection  = l.head(2);
    var res3 : string             = l.last(0);
    var res6 : string             = l.nth(0);
    var res7 : string collection  = l.tail(2);
    require (true)
  }
}
