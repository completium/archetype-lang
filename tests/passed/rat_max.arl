archetype rat_min

variable res : rational = 0.0

entry exec () {
  effect {
    res := max(1 / 2, 1 / 3);
    dorequire(res = 1 / 2, "INVALID_VALUE_1");

    res := max(1 / 2, 2 / 5);
    dorequire(res = 1 / 2, "INVALID_VALUE_2")
  }
}
