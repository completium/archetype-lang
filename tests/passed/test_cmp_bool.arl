archetype test_cmp_bool

entry exec() {
  effect {
    dorequire (true = true);
    dorequire ((true = true) = true);

    var a : bool = true;
    var b : bool = false;
    dorequire (a <> b);
  }
}
