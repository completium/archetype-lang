archetype expr_fun_max_rat_int

variable res : rational = 0 div 1

entry exec () {
  specification {
    s0: res = 1;
  }
  effect {
    var a : rational = 1 div 2;
    var b : int = 1;
    res := max(a, b)
  }
}
