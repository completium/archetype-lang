archetype expr_formula_asset_method_head

asset my_asset identified by id {
  id : string;
  value : int;
} initialized by {
  {"id0"; 0};
  {"id1"; 1};
  {"id2"; 2}
}

action exec () {

  specification {
    s: let some a = my_asset.head(2) in true otherwise false;
  }

  effect {
    require (true)
  }
}
