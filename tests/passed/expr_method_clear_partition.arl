archetype expr_method_clear_partition

asset o_asset identified by oid {
  oid : string;
}

asset my_asset identified by id {
  id     : string;
  value  : int;
  col    : o_asset partition;
} initialized by {
  {"id0"; 0; [{"oid0"}; {"oid1"}; {"oid2"}]};
  {"id1"; 1; []};
  {"id2"; 2; []}
}

variable res : int = 0

action exec () {
  effect {
    my_asset.get("id0").col.clear()
  }
}
