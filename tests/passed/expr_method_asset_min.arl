archetype expr_method_asset_min

asset my_asset identified by id {
  id : string;
  value : int;
} initialized by {
  {"id0"; 1};
  {"id1"; 1};
  {"id2"; 2}
}

variable res : int = 0

action exec () {
  specification {
    s0: res = 1;
  }
  effect {
    res := my_asset.min(the.value)
  }
}
