archetype get_in_require_failif

asset myasset {
  id: string;
  value: int;
}

action exec () {
  require {
    r1: myasset["myid"].value = 0;
  }
  failif {
    f1: myasset["myid"].value <> 0;
  }
  effect {
    let a : int = 0 in
    a := 1
  }
}
