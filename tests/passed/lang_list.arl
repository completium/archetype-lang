archetype lang_list

action exec () {
  effect {
    var l : string list = ["1"; "2"; "3"];

    (* expr *)
    var a : string list  = l.prepend("2");
    var c : bool         = l.contains("2");
    var k : int          = l.count();
    var n : string       = l.nth(0);

  }
}
