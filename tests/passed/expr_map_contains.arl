archetype expr_map_contains

variable res : bool = false

entry exec () {
  specification {
    s0: res;
  }
  effect {
    var m : map<int, string> = [(0, "0")];
    res := map_contains(m, 0);
  }
}
