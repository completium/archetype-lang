archetype expr_fun_floor

variable r0 : int = 0
variable r1 : int = 0

entry exec () {
  specification {
    s0: r0 = 1 and r1 = -2;
  }
  effect {
    r0 := floor(5 / 3);
    r1 := floor(-5 / 3);
  }
}
