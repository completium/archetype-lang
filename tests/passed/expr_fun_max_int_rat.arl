archetype expr_fun_max_int_rat

variable res : rational = 0 div 1

entry exec () {
  specification {
    s0: res = 1;
  }
  effect {
    var a : int = 1;
    var b : rational = 1 div 3;
    res := max(a, b)
  }
}
