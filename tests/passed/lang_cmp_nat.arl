archetype lang_cmp_nat

variable res : bool = false

entry exec () {
  effect {
    var a : nat = 1;
    var b : nat = 2;

    res := a =  b;
    res := a <> b;
    res := a <  b;
    res := a <= b;
    res := a >  b;
    res := a >= b;

    res := a =  2;
    res := a <> 2;
    res := a <  2;
    res := a <= 2;
    res := a >  2;
    res := a >= 2;

    res := 1 =  b;
    res := 1 <> b;
    res := 1 <  b;
    res := 1 <= b;
    res := 1 >  b;
    res := 1 >= b;

    res := 0.5 =  b;
    res := 0.5 <> b;
    res := 0.5 <  b;
    res := 0.5 <= b;
    res := 0.5 >  b;
    res := 0.5 >= b;

    res := a =  1.5;
    res := a <> 1.5;
    res := a <  1.5;
    res := a <= 1.5;
    res := a >  1.5;
    res := a >= 1.5;
  }
}
