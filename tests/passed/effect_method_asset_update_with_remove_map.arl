archetype effect_method_asset_update_with_remove_map

asset my_asset {
  id : nat;
  m : map<string, bytes>;
} initialized by {
  {0; [("k", 0x00)]}
}

entry exec (n : nat) {
  my_asset.update(0, {m -= "k"});
}
