archetype record_container

record my_record {
  s: set<nat>;
  l: list<nat>;
  m: map<nat, string>;
}

// variable vs: set<my_record> = []
variable vl: list<my_record> = []
variable vm: map<nat, my_record> = []

entry exec () {
  ()
}
