archetype effect_method_asset_clear_asset_one_field

asset my_asset identified by id {
  id : string;
} initialized by {
  {"id0"};
  {"id1"};
  {"id2"}
}

variable res : int = 0

entry exec () {
  effect {
    my_asset.clear()
  }
}
