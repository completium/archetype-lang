archetype nested_for

asset myasset {
  id : int;
  v : string;
}

action exec () {
  effect {
    for c in myasset do
      for d in myasset do
        require(myasset[c].v = myasset[d].v)
      done
    done
  }
}
