archetype assign_field

asset myasset identified by id {
  id : int;
  str : string;
} initialized by {
  {0; ""}
}

action exec () {
  specification {
    s0: myasset[0].str = "mystr"
  }
  effect {
    myasset[0].str := "mystr";
    require (myasset[0].str = "mystr")
  }
}
