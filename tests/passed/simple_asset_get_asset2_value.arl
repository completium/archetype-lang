archetype simple_asset_get_asset2_value

asset my_asset identified by id {
  id : string;
  v0 : nat;
  v1 : nat;
} initialized by {
  {"id0"; 0; 0};
  {"id1"; 1; 1};
  {"id2"; 2; 2}
}


variable res : nat = 0

entry exec () {
  var v = my_asset["id1"].v0;
  dorequire(v = 1, "ko");
  res := v;
}
