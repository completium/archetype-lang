archetype expr_method_asset_head_one_field

asset my_asset identified by id {
  id : string;
} initialized by {
  {"id0"};
  {"id1"};
  {"id2"}
}

variable res : string = ""

entry exec () {
  var l = my_asset.head(2);
  var k ?= l.nth(1) : "OUT_OF_BOUND";
  res := k
}
