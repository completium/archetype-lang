archetype expr_fun_unpack_bool

variable res : option<bool> = none

entry exec () {
  specification {
    s0: is_some(res);
    s1: let some r = get_some(res) in r otherwise false;
  }
  effect {
    res := unpack<bool>(0x05030a);
    do_require (get_some(res), "ko")
  }
}
