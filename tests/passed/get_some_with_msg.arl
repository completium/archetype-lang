archetype get_some_with_msg

variable res : nat = 0

entry exec() {
  var o : option<nat> = some(1);
  // res := o ? the : fail(0);
  // fail_none(o)
  res := require_some(o, "ERROR");
  // res := get_some_with_message(o, "ERROR");
  // do_require(res = 2, "INVALID_VALUE")
}
