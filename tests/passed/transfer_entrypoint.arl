archetype transfer_entrypoint

variable res : nat = 0

entry set_value(n : nat) {
  res := n
}

entry exec () {
  var n : nat = res + 1;

  var eopt : option<contract<nat>> = get_entrypoint<nat>("%set_value", self_address);
  var e = get_some(eopt);
  transfer 0tz to entry e(n);
}
