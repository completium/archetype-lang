archetype test_addfield_aggregate_2

asset o_asset identified by oid {
  oid : nat;
} initialized by {
  {0};
  {1};
  {2}
}

asset my_asset identified by id {
  id     : string;
  col    : aggregate<o_asset>;
  val    : bool;
} initialized by {
  {"id0"; []; true};
  {"id1"; []; true};
  {"id2"; []; true}
}

entry exec (n : nat) {
  my_asset["id0"].col.add(n)
}
