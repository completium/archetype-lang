archetype simple_fun6

variable res : int = 0

function f (i : int) : int {
  return i
}

function g (i : int) : int {
  return (i + 1)
}

entry exec () {
  res := f(0);
}

entry exec1 () {
  res := f(1);
}

entry exec2 () {
  res := f(2);
}
