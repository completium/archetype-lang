archetype expr_method_asset_sort

asset my_asset identified by id {
  id : string;
  value : int;
} initialized by {
  {"id0"; 3};
  {"id1"; 2};
  {"id2"; 1}
}

variable res : int = 0

action exec () {
  effect {
    res := my_asset.sort(value).nth(0).value
  }
}
