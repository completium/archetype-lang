archetype test_tuple_access_2

variable rid : string = ""
variable rv  : int    = 0i
variable rb  : bool   = false

entry exec () {
  var lit1 : (string * int * bool) = ("mystr", 2i, true);
  rid := lit1[0];
  rv  := lit1[1];
  rb  := lit1[2]
}
