archetype expr_list_prepend

variable res : string list = []

action exec () {
  specification {
    s0: res.count () = 4;
  }
  effect {
    var l : string list = ["1"; "2"; "3"];
    res := prepend(l, "0");
  }
}
