archetype test_asset_access

asset one_asset {
  oid : int;
} initialized by {
  {8}
}

asset my_asset {
  id : int;
  v  : string;
} initialized by {
  {1; "mystr"}
}

action exec() {
  effect {
    var a : my_asset = my_asset.get(0);
    require (a.id = 0);
    var str = a.v;
    fail(str)
  }
}
