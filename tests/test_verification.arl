archetype test_verification

action myaction = {
 verification {

    definition mydef =
     { x : early | forall x in col1, x = x }

    predicate mypredicate (a : int) = {
      forall x in col1, forall y in col2, x = y
    }

    lemma myaxiom = {
      forall x in col1, forall y in col2, x = y
    }

    theorem mytheorem = {
      forall x in col1, forall y in col2, x = y
    }

    variable myvar atype = 0

    effect {
      x := y
    }

    assert a1 at aaa = {
      x = y
      invariants for myloop {
        x = 0;
        y = 0
      }
    }

    specification s1 = {
      x = y
      invariants for myloop {
        x = 0;
        y = 0
      }
    }
  }
}
