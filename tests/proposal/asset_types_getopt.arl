archetype asset_types

asset abc {
  a : nat;
  b : string;
  c : bytes;
} initialized by {
  { 0; "mystr"; 0x }
}

entry get_value() {
  const ov : option<asset_value<abc>> = abc.getopt(0);
  const v = match ov with | some(v) -> v.b | none -> "" end;
  // dorequire(v = "mystr", "INVALID_VALUE")
}
