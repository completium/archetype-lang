archetype test_asset_update_all_coll_0

asset my_asset {
  mid : string;
  v : nat
} initialized by {
  {"id0"; 0};
  {"id1"; 1};
  {"id2"; 2}
}

variable res : list<nat> = []

entry exec (n : nat) {
  my_asset.update_all({ v += 2 })
}
