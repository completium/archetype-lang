archetype asset_access_ternary_not_found

asset abc {
  a : nat;
  b : string;
  c : bytes;
} initialized by {
  { 0; "mystr"; 0x }
}


entry get_value() {
  const v : string = abc[1]? b : "not_found";
  dorequire(v = "not_found", "INVALID_VALUE");
}
