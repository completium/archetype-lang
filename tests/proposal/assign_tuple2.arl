archetype assign_tuple2

variable res : (nat * (nat * string * bytes) * bytes) = (0, (0, "mystring", 0x0001), 0x0001)

entry exec () {
  effect {
    res[1][1] := "str";
    dorequire (res = (0, (0, "str", 0x0001), 0x0001), "ko")
  }
}
