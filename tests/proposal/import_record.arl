archetype import_contract

import my_contract from "./tests/proposal/import_decl.arl"

variable res : my_contract::my_record = my_contract::{ n = 0; s = ::myc }

variable myc : nat = 2

entry exec () {
  const r : my_contract::my_record = import_decl::{ n = myc; s = "mystr" };
  res := r
}
