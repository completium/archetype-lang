archetype spec_fail_caller

variable admin : address = @tz1

entry exec () {
  specification {
    fails  {
      f1 with InvalidCaller(msg : string) :
        caller <> admin
    }
  }
  called by admin
  effect {
    ()
  }
}
