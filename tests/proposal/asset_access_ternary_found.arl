archetype asset_access_ternary_found

asset abc {
  a : nat;
  b : string;
  c : bytes;
} initialized by {
  { 0; "mystr"; 0x }
}


entry get_value() {
  const v : string = abc[0]?b:"not_found";
  dorequire(v = some("mystr"), "INVALID_VALUE");
}
