# Unit
{
  storage unit;
  parameter (contract %export_ticket (list (ticket (pair nat (option bytes)))));
  code { NIL operation;
         DIG 1;
         UNPAIR;
         NIL (ticket (pair nat (option bytes)));
         NIL operation;
         DUP 3;
         PUSH mutez 0;
         DUP 4;
         TRANSFER_TOKENS;
         CONS;
         DIP { DIG 3; DROP };
         DUG 3;
         DROP 2;
         DIG 1;
         PAIR };
}

