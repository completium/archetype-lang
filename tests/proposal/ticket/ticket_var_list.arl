archetype ticket_var_list

variable info : option<(address * string * nat)> = none

entry exec() {
  const lt : list<ticket<string>> = [create_ticket("info", 1)];
  const t ?= match lt with | t::l -> some(t) | [] -> none<ticket<string>> end;
  info := some(read_ticket(t))
}
