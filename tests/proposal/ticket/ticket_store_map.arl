archetype ticket_store_map

variable mt : map<nat, ticket<string>> = []

variable info : option<(address * string * nat)> = none

entry exec() {
  const ti ?= create_ticket("info", 1) : "ERROR";
  mt.update(0, ti);

  // detach t from mt[0] : "ERROR";
  info := some(read_ticket(ti))
}
