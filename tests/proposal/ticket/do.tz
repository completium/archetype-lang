# (Pair None 2)
{
  storage (pair (option %input (ticket string)) (nat %n));
  parameter (unit %exec);
  code { UNPAIR;
         DIP { UNPAIR 2 };
         DROP;
         PUSH (option (ticket string)) None;
         SWAP;
         IF_NONE
           { PUSH string "ERROR";
             FAILWITH }
           {  };
         DROP;
         PAIR 2;
         NIL operation;
         PAIR };
}

