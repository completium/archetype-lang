archetype detach_list

variable input : list<ticket<string>> = []

variable output : option<ticket<string>> = none<ticket<string>>

entry init() {
  const ti ?= create_ticket("info", 1) : "ERROR";
  input := [ti];
}

entry exec() {
  detach t from input.nth(0) : "ERROR";
  output := some(t)
}
