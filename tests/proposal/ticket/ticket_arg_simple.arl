archetype ticket_arg_simple

variable info : option<(address * string * nat)> = none

entry callback(t : ticket<string>) {
  info := some(read_ticket(t))
}

entry exec() {
  const t : ticket<string> = create_ticket("info", 1);
  transfer 0tz to entry self.callback(t)
}
