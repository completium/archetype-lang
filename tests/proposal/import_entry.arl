archetype import_entry

import "./import_decl.arl"

variable res : nat = 0

variable myc : string = ""

entry exec (a : address) {
  const r : import_decl::my_record = import_decl::{ n= 0; s = ::myc };
  transfer 0tz to entry import_decl(a).e2(r);
}
