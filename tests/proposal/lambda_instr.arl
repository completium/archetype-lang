archetype lambda_instr

variable res : string = ""

entry exec () {
  var f : lambda<nat, string> = lambda<string>((x : nat) ->
    if (x = 0)
    then fail ("Zero");
    return "str");

  res := exec_lambda(f, 1)
}
