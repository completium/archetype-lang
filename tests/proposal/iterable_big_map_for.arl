archetype iterable_big_map_for

variable my_map : iterable_big_map<string, nat> = [ ("mystr0", 0) ; ("mystr1", 1) ; ("mystr2", 2) ; ("mystr3", 3) ]

entry exec () {
  var res : nat = 0;
  for (k, v) in my_map do
    if (k <> "")
    then res += v;
  done;
  dorequire (res = 6, "ko")
}
