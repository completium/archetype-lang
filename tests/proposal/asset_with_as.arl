archetype asset_with_as

asset myasset identified by a b c as (((x, y), z)) {
  a : nat;
  b : string;
  c : bytes;
  d : string;
  e : nat;
  f : string;
  g : bytes;
}
as (((a, b), (c, d)))

entry exec() {

}
