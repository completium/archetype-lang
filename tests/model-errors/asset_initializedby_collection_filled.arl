archetype asset_initializedby_collection_filled

asset o_asset {
  oid: int;
}

asset my_asset identified by id {
  id : string;
  o  : o_asset collection;
} initialized by {
  {"my_id"; [{0}]}
}

action exec () {
  effect {
    fail ("nothing")
  }
}
