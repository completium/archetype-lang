archetype ticket_get_map

variable mt : map<nat, ticket<string>> = []

variable res : option<ticket<string>> = none

entry init() {
  const ot = create_ticket("info", 1);
  mt.update(0, ot);
}

entry exec() {
  res := mt[0] // value of mt is a linear type, must be rejected
}
