# (Pair 0 (Pair 0 0))
{
  storage (pair (nat %sto_1) (pair (nat %sto_2) (nat %sto_3)));
  parameter (nat %default);
  code { UNPAIR;
         DIP { UNPAIR 3 };
         DUP;
         DIP { DIG 2; DROP };
         DUG 2;
         DROP;
         PAIR 3;
         NIL operation;
         PAIR };
}
