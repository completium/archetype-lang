model extension mutable (

  variable[%mutable arole cond] avariable type

) = {

  variable avariable type

  transaction set_<%avariable> = {
    args = {
        newvariable : type;
    }
    called by arole;
    condition : cond;
    action = { avariable := newvariable }
  }
}
