(env
  (dev
    (flags (:standard -rectypes -g -w Y -w Z -w -9 -w -23 -w +28 -w +33 -w -32)))
  (release
    (flags (:standard -rectypes))))

(ocamllex lexer)
(menhir (flags --explain) (modules parser))

(library
  (name cmlLib)
  (preprocess
    (pps ppx_deriving.std)
  )
(library_flags (-linkall))
  (modules core ident location parseUtils parseTree parser lexer io model modelinfo modelws modelw3liq printer translate liq_printer)
  (libraries menhirLib why3 ppx_deriving)
)

(library
  (name w3plugin)
  (preprocess
    (pps ppx_deriving.std)
  )
  (library_flags (-linkall))
  (modules w3plugin)
  (libraries cmlLib menhirLib why3 ppx_deriving)
)

(executable
  (preprocess
    (pps ppx_deriving.std)
  )
  (modules compiler)
  (libraries cmlLib menhirLib why3 ppx_deriving)
  (name     compiler))
