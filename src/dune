(env
  (dev
    (flags (:standard -g -w Y -w Z -w -9 -w -23 -w +28 -w +33 -w -32)))
  (release
    (flags (:standard))))

(ocamllex lexer)
(menhir (flags --explain) (modules parser))

(executables
  (modes     byte)
  (preprocess
    (pps ppx_deriving.std)
  )
  (libraries batteries menhirLib why3 ppx_deriving)
  (names     compiler genwhy3 genmodelws))
