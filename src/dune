(ocamllex lexer)
(menhir
  (modules parser)
  (flags --explain --inspection --table --dump))

(library
  (name archetype)
  (public_name archetype)
  (preprocess
    (pps ppx_deriving.std ppx_deriving_yojson)
  )
  (library_flags (-linkall))
  (modules tools core ident options location parseUtils parseTree parser lexer symbol error position pureLexer parseError io printer typing ast reduce model gen_model model_wse remove_se mltree gen_liquidity gen_why3 gen_markdown printer_mltree mlwtree printer_mlwtree printer_whyml)
  (libraries menhirLib omd why3 ppx_deriving yojson ppx_deriving_yojson.runtime)
)

(library
  (name w3plugin)
  (modules w3plugin)
  (libraries archetype)
)

(rule
 (targets archetype_plugin.cmxs)
 (deps    archetype.cmxa w3plugin.cmxa)
 (action  (run ocamlopt -shared -linkall -o %{targets}
            %{lib:easy-format:easy_format.cmxa}
            %{lib:biniou:biniou.cmxa}
            %{lib:yojson:yojson.cmxa}
            %{lib:result:result.cmxa}
            %{lib:ppx_deriving:ppx_deriving_runtime.cmxa}
            %{lib:ppx_deriving_yojson:runtime/ppx_deriving_yojson_runtime.cmxa}
            %{deps} )))

(executable
  (preprocess
    (pps ppx_deriving.std ppx_deriving_yojson)
  )
  (modules lsp compiler)
  (libraries archetype menhirLib why3)
  (public_name archetype)
  (name     compiler))
