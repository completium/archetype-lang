(ocamllex lexer)
(menhir (flags --explain) (modules parser))

(library
  (name archetype)
  (public_name archetype)
  (preprocess
    (pps ppx_deriving.std ppx_deriving_yojson)
  )
  (library_flags (-linkall))
  (modules tools core ident location parseUtils parseTree parser lexer io model modelinfo modelws modelliq printer translate reduce liq_printer)
  (libraries menhirLib why3 ppx_deriving yojson ppx_deriving_yojson.runtime)
)

(library
  (name w3plugin)
  (modules w3plugin)
  (libraries archetype)
)

(rule
 (targets archetype_plugin.cmxs)
 (deps    archetype.cmxa w3plugin.cmxa)
 (action  (run ocamlopt -shared -linkall -o %{targets}
            %{lib:easy-format:easy_format.cmxa}
            %{lib:biniou:biniou.cmxa}
            %{lib:yojson:yojson.cmxa}
            %{lib:result:result.cmxa}
            %{lib:ppx_deriving:ppx_deriving_runtime.cmxa}
            %{lib:ppx_deriving_yojson:runtime/ppx_deriving_yojson_runtime.cmxa}
            %{deps} )))

(executable
  (preprocess
    (pps ppx_deriving.std ppx_deriving_yojson)
  )
  (modules extract lsp compiler)
  (libraries archetype menhirLib why3)
  (public_name archetype)
  (name     compiler))
