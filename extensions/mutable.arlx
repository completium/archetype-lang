archetype extension mutable (

  variable[%mutable arole cond] avariable type

) = {

  action set_<%avariable> (newvariable : type) = {
    called by arole
    condition set_<%avariable>_c1 : cond
    effect
      avariable := newvariable
  }
}
