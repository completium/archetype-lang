{ storage (list (or nat string)) ;
  parameter (unit %exec) ;
  code { CDR ;
         DUP ;
         PUSH nat 1 ;
         LEFT string ;
         CONS ;
         SWAP ;
         DROP 1 ;
         NIL operation ;
         PAIR } }
