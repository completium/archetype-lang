{ storage unit ;
  parameter (unit %exec) ;
  code { LAMBDA
           unit
           nat
           { PUSH unit Unit ; PUSH nat 1 ; SWAP ; DROP 1 ; SWAP ; DROP 1 } ;
         SWAP ;
         CDR ;
         NIL operation ;
         PAIR ;
         DIP 1 { DROP 1 } } }
