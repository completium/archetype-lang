{ storage (pair (nat %id) (map %m nat string)) ;
  parameter (unit %exec) ;
  code { CDR ;
         UNPAIR ;
         SWAP ;
         PUSH string "0" ;
         SOME ;
         PUSH nat 0 ;
         UPDATE ;
         SWAP ;
         PAIR ;
         NIL operation ;
         PAIR } }
