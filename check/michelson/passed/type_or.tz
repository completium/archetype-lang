{ storage
    (pair (or %ls nat string)
          (or %lc nat string)
          (or %rs nat string)
          (or %rc nat string)) ;
  parameter (unit %exec) ;
  code { UNPAIR ; DIP 1 { UNPAIR 4 } ; DROP 1 ; PAIR 4 ; NIL operation ; PAIR } }
