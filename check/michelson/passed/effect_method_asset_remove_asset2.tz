{ storage (map string (pair (int %value) (bool %v))) ;
  parameter (unit %exec) ;
  code { CDR ;
         DUP ;
         NONE (pair int bool) ;
         PUSH string "id0" ;
         UPDATE ;
         SWAP ;
         DROP 1 ;
         NIL operation ;
         PAIR } }
