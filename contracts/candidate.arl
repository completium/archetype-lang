// this is a simple test contract to register an address
archetype candidate

variable candidate : address = @kt1

variable caller_contract : address = @KT1

action register_candidate (addr : address) {
    called by caller_contract
    effect {
        candidate := addr
    }
}