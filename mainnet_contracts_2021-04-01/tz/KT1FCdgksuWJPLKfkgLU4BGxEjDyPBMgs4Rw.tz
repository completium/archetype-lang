{
  storage (list (pair nat (pair string (pair string (pair nat (map string string))))));
  parameter (list (pair nat (pair string (pair string (pair nat (map string string))))));
  code { CAR;
         NIL operation;
         PAIR };
}
