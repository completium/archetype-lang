archetype name_scope

asset toto {
  toto: string;
}

action toto () {
  effect {
    var toto = "str";
    var l = toto.select(the.toto = toto); (* `the` is now mandatory *)
    require (true)
  }
}
