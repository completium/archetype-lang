{
  storage int;
  parameter (or (int %decrement) (int %increment));
  code { UNPAIR;
         IF_LEFT
           { SWAP;
             SUB }
           { ADD };
         NIL operation;
         PAIR };
}
